import React from "react";
import TwoColumnData from "../Transaction/TwoColumnData";

const GeneralAccount = ({ rows }) => {
  return (
    <TwoColumnData rows={rows} />
  );
};

export default GeneralAccount;
